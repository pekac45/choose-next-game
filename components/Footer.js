import Link from 'next/link';
import { Grid, Cell } from 'styled-css-grid';
import { HeaderWrapper, LinkWrapper, FooterWrapper } from '../styles/components';

const Header = () => (
  <FooterWrapper>
    <p>Footer</p>
  </FooterWrapper>
);

export default Header;
